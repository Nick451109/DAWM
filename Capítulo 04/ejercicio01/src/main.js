/*typescript es un superconjunto de js que transpila codigo a codigo Javascript
ofrece clases, modulo e interfaces (tipado fuerte)
los checkeos los hace en tiempo de compilacion
instalar globalmente con: npm install -g typescript
instalar localmente con  :npm install --save-dev typescript (evita posibles interferencias con otros proyectos TS)

tsconfig.json define las configuraciones del proyecto, las opciones del compilador y los archivos que seran incluidos

*/
function holaMundo() {
    console.logg("Hola, mundo!");
}
holaMundo();
